export interface Books {
   /* bib_key: string,
    preview: string,
    thumbnail_url: string,
    preview_url: string,
    info_url: string, */

    kind : string,
    totalItems : string,
    items: Array<string>,
}
